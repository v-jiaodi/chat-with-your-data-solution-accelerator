# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: chat-with-your-data-solution-accelerator
metadata:
  template: chat-with-your-data-solution-accelerator@0.0.2-beta
services:
  webadmin:
    project: ./backend
    language: py
    host: appservice
  frontend:
    project: ./backend
    language: js
    host: appservice
  function:
    project: ./backend
    language: py
    host: function